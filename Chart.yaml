apiVersion: v2
name: my-helm-application
version: 0.1.0
dependencies:
  - name: nextcloud
    version: "4.3.1" # Specify the exact version of the Nextcloud chart you want to use.
    repository: "https://nextcloud.github.io/helm/"
